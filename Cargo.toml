[package]
name = "spindle"
version = "0.1.0"
edition = "2021"
description = "A Rust terminal multiplexer for persistent parallel coding-agent sessions"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
