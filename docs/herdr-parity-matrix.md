# Herdr behavior parity matrix

Reference: `C:\Users\Palguna\.opensrc\repos\github.com\herdrdev\herdr\master`.
Statuses describe the current Spindle checkout, not the target. “Partial” means
some server/model support exists but the user-facing behavior is missing or
does not yet match Herdr.

| User behavior | Spindle status | Herdr reference | Evidence / next verification |
| --- | --- | --- | --- |
| Start or attach to a persistent session | Partial | `docs/preview/website/src/content/docs/quick-start.mdx`, `src/workspace.rs` (`Workspace::new`) | Herdr creates its first shell with the workspace. Spindle requests it on attach, verifies the resulting focused pane, and recovers/reports an error rather than silently showing a connected empty canvas. Integration coverage exercises attach → ensure → focused-pane snapshot and failed-start recovery; live TUI and warm-reattach checks remain. |
| Start with a live shell; create shell-backed tabs/workspaces | Partial | `src/workspace.rs` (`Workspace::new`, `create_tab_with_runtime`) | Spindle now ensures a shell after attach, container creation, and switching, using the active workspace repository path. Integration test covers initial, tab, workspace, and space shell creation, idempotency, and focus; live TUI verification remains. |
| Navigate spaces/workspaces and tabs in a sidebar | Partial | `src/client/shell/sidebar.rs`, `src/client/shell/workspace_navigation.rs`, Herdr `src/client/shell/config.rs`, `src/client/shell/preferences.rs` | Spindle renders a space/workspace sidebar and tab strip; click and keyboard navigation switch containers. `Ctrl-b b` and the sidebar footer toggle Herdr's compact four-column mode, where space/workspace markers remain clickable; the choice is saved per project under the local state directory. Resizing and larger-list scrolling remain. |
| Render pane layouts with focus, terminal state, correct geometry | Partial | `src/ui/panes.rs`, `src/layout.rs` | Shared pane rectangles now drive drawing, clicks, and per-PTY sizes; layout changes and terminal resizes recalculate each shell's inner dimensions. Small-terminal/many-pane geometry tests pass. Fresh Windows Terminal verification remains. |
| Click panes/tabs/workspaces; drag split borders | Partial | `src/client/shell/mouse.rs`, `src/layout.rs` | Spindle handles clicks and divider drags. Drag updates the exact nested split by tree path, saves the ratio, and triggers per-pane PTY resizing. Unit and server-client tests cover hit paths and persisted nested ratios; manual Windows Terminal verification remains. |
| Right-click context menus for pane/tab/workspace actions | Partial | `src/client/shell/context_menu.rs` | Spindle opens target-specific menus with keyboard and mouse selection, bounded placement, and tested small-terminal scrolling. Pane split, close, rename, focus, toggle-zoom, and per-pane right-click passthrough are implemented. Herdr-only worktree actions remain missing. |
| Select/copy terminal text and route terminal mouse input | Partial | `src/selection.rs`, `src/client/shell/mouse.rs`, `src/server/pane_input.rs` | Spindle supports visible-screen selection, double-click word selection, and copy; wheel and mode-aware PageUp/PageDown scrolling expose up to 4,096 rows reconstructed from its bounded recent PTY history. Page keys follow Herdr's alternate-screen, mouse-reporting, application-cursor, and bracketed-paste rule; mouse input requested by apps still takes priority. Unlike Herdr's backend scroll model, history is limited to Spindle's recent output byte ring and uses current pane dimensions; scrollbar polish and live Windows Terminal verification remain. |
| Discover and use keyboard and mouse controls | Partial | `src/main.rs` `[keys]`, `docs/preview/website/src/content/docs/keyboard.mdx`, `src/client/shell/context_menu.rs` | Spindle now matches Herdr's defaults for `Ctrl-b c` new tab, `Ctrl-b x` close pane, `Ctrl-b Shift+x` close tab, `Ctrl-b n/p` tab navigation, `Ctrl-b v/-` splits, `Ctrl-b h/j/k/l` pane focus, and `Ctrl-b z` zoom. Herdr exposes right-click passthrough in the pane menu but has no default key binding; Spindle also makes it available in the keyboard command palette. Its in-app guide lists the shortcuts. Configurable bindings and manual Windows verification remain. |
| Show detected agent identity and unknown/idle/working/blocked state | Partial | `src/detect.rs`, Herdr `src/detect/manifests/codex.toml`, `src/detect/manifests/opencode.toml` | Windows process-tree lookup identifies direct Codex/OpenCode executables, Codex npm command shims, and known Node entry points. Pane titles and workspace counts show agent state. OpenCode permission detection follows Herdr's control hints and interrupt line, avoiding blocks on generic permission text. Codex directory-trust detection now requires Herdr's visible directory header; blocker prompts include selected manifest signals, and interrupted turns suppress the working fallback. Full manifests/transitions, multi-agent sidebar detail, and live side-by-side verification remain. |
| Configure themes, sidebar, keybindings, and notifications | Missing / partial | `src/config/`, `src/client/notifications.rs`, `src/client/shell/notifications.rs` | Spindle keybindings are static and has no settings UI. Audit exact behavior and test config/default fallback. |
| Agent integrations, CLI automation, plugins, and remote sessions | Missing | `src/integration/`, `src/cli/`, `src/app/api/plugins/`, `src/remote/` | Spindle is local-only and has no integration/plugin system. Inventory command and API behavior before choosing port details. |
| Windows-first process, terminal, installer, and recovery behavior | Partial | Herdr `src/platform/windows.rs`, `src/pty/backend.rs`, `scripts/windows_*` | Spindle has Windows PTY and smoke tests; add side-by-side process/input/recovery checks and record concrete improvements. |

Update a row only after checking the matching Herdr code and testing the
corresponding Spindle behavior. Any deliberate difference needs a reason and a
usable alternative, not just a missing implementation.
