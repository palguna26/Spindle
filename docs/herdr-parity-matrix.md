# Herdr behavior parity matrix

Reference: `C:\Users\Palguna\.opensrc\repos\github.com\herdrdev\herdr\master`.
Statuses describe the current Spindle checkout, not the target. “Partial” means
some server/model support exists but the user-facing behavior is missing or
does not yet match Herdr.

| User behavior | Spindle status | Herdr reference | Evidence / next verification |
| --- | --- | --- | --- |
| Start or attach to a persistent session | Partial | `docs/preview/website/src/content/docs/quick-start.mdx`, `src/workspace.rs` (`Workspace::new`), `src/pane/terminal.rs` | Herdr creates its first shell with the workspace and answers terminal queries through its emulator. Spindle restarts saved `Interrupted` panes in place and answers PowerShell's cursor-position query. Integration tests cover both; live Windows verification showed the prompt, command input/output, detach, and reattach to the same pane with output intact. Fresh verification inside Windows Terminal remains. |
| Start with a live shell; create shell-backed tabs/workspaces | Partial | `src/workspace.rs` (`Workspace::new`, `create_tab_with_runtime`) | Herdr creates the initial shell with the workspace. Spindle ensures one after attach and container creation, using the active workspace repository path; concurrent ensure calls are tested to produce one pane. Integration tests cover tab/workspace/space creation, idempotency, and focus; live Windows TUI verification remains. |
| Navigate spaces/workspaces and tabs in a sidebar | Partial | `src/client/shell/sidebar.rs`, `src/client/shell/workspace_navigation.rs`, Herdr `src/client/shell/config.rs`, `src/client/shell/preferences.rs`, `src/client/shell/sidebar.rs`, `src/client/shell/mouse.rs` | Spindle renders a space/workspace sidebar and tab strip; click and keyboard navigation switch containers. `Ctrl-b b` and the sidebar footer toggle Herdr's compact four-column mode, where space/workspace markers remain clickable; the choice is saved per project under the local state directory. Long lists scroll with the wheel; track clicks jump and thumb drags preserve the grab offset, following Herdr. Resize-driven layout polish and live verification remain. |
| Render pane layouts with focus, terminal state, correct geometry | Partial | `src/ui/panes.rs`, `src/layout.rs` | Shared pane rectangles drive drawing, clicks, and per-PTY sizes. Spindle now propagates terminal cursor visibility and positions the host cursor in the focused pane; it hides the cursor for overlays and scrollback, matching Herdr's focused/live-pane rule. Cursor placement and small-terminal/many-pane geometry tests pass. Fresh Windows Terminal verification remains. |
| Click panes/tabs/workspaces; drag split borders | Partial | `src/client/shell/mouse.rs`, `src/layout.rs` | Spindle handles clicks and divider drags. Drag updates the exact nested split by tree path, saves the ratio, and triggers per-pane PTY resizing. Unit and server-client tests cover hit paths and persisted nested ratios; manual Windows Terminal verification remains. |
| Right-click context menus for pane/tab/workspace actions | Partial | Herdr `src/workspace.rs` (`remove_pane`); `src/app/actions.rs` (`close_selected_workspace`); `src/app/api/tabs.rs` (`handle_tab_close`); `src/client/shell/context_menu.rs` | Spindle removes a tab when its last pane closes and a sibling tab exists; closing the final tab or pane closes that workspace, selects a sibling workspace if available, or shows an empty state with a create-workspace shortcut. Workspace focus is optional and the empty state survives server restart; integration tests cover closing, restart, and recovery. Pane split, rename/clear-name, focus, swap-with-focused, toggle-zoom, and right-click passthrough are implemented. Empty tabs now explain shell/tab recovery. Unit tests cover swap layout/focus and menu visibility; live Windows Terminal verification remains. Herdr-only worktree actions remain missing. |
| Select/copy terminal text and route terminal mouse input | Partial | `src/selection.rs`, `src/client/shell/mouse.rs`, `src/server/pane_input.rs` | Spindle supports visible-screen selection, double-click word selection, and copy; wheel and mode-aware PageUp/PageDown scrolling expose up to 4,096 rows reconstructed from its bounded recent PTY history. Page keys follow Herdr's alternate-screen, mouse-reporting, application-cursor, and bracketed-paste rule; mouse input requested by apps still takes priority. Unlike Herdr's backend scroll model, history is limited to Spindle's recent output byte ring and uses current pane dimensions; scrollbar polish and live Windows Terminal verification remain. |
| Discover and use keyboard and mouse controls | Partial | `src/main.rs` `[keys]`, `docs/preview/website/src/content/docs/keyboard.mdx`, `src/client/shell/context_menu.rs` | Spindle now matches Herdr's defaults for `Ctrl-b c` new tab, `Ctrl-b x` close pane, `Ctrl-b Shift+x` close tab, `Ctrl-b n/p` tab navigation, `Ctrl-b v/-` splits, `Ctrl-b h/j/k/l` pane focus, and `Ctrl-b z` zoom. Herdr exposes right-click passthrough in the pane menu but has no default key binding; Spindle also makes it available in the keyboard command palette. Its in-app guide lists the shortcuts. Configurable bindings and manual Windows verification remain. |
| Show detected agent identity and unknown/idle/working/blocked state | Partial | `src/detect.rs`, `src/pane/manager.rs`, Herdr `src/detect/manifests/claude.toml`, `codex.toml`, `opencode.toml`, `src/pane/agent_detection.rs`, `src/client/shell/agent_sidebar.rs` | Spindle identifies direct Claude/Codex/OpenCode process names plus Claude PowerShell/command shims. Claude detection uses Herdr's OSC 9 idle progress, ✳ idle title, spinner/interrupt working, and high-confidence blocker cues; OSC title/progress evidence is cleared when the detected agent changes. Plain working-to-idle transitions follow Herdr's 100 ms recheck, three confirmations, and 700 ms cap; visible idle signals, agent changes, and process exits bypass the hold. OpenCode permission detection follows Herdr's control hints and interrupt line; Codex trust requires Herdr's visible directory header. Each detected pane now has a focused/highlighted sidebar row, grouped under its workspace/tab; blocked agents are listed before working, idle, and unknown peers. Selecting a row switches to and focuses that pane. Full manifest coverage, a global priority-sort toggle, and live side-by-side verification remain. |
| Configure themes, sidebar, keybindings, and notifications | Missing / partial | Spindle `src/config/`, `src/client/app.rs`, `src/client/renderer.rs`; Herdr `src/app/actions.rs`, `src/ui.rs` | Spindle shows request failures in a brief TUI toast, following Herdr's non-blocking toast feedback. Notification delivery settings, desktop notifications, configurable keybindings, themes, and a settings UI remain missing. |
| Agent integrations, CLI automation, plugins, and remote sessions | Missing | `src/integration/`, `src/cli/`, `src/app/api/plugins/`, `src/remote/` | Spindle is local-only and has no integration/plugin system. Inventory command and API behavior before choosing port details. |
| Windows-first process, terminal, installer, and recovery behavior | Partial | Herdr `src/platform/windows.rs`, `src/pty/backend.rs`, `src/session.rs` (`stop_socket_with_timeout`), `scripts/windows_*` | Spindle's `stop` now waits for endpoint cleanup; Windows named-pipe wake retries handle the gap between requests. Release Windows smoke passes stale-endpoint recovery, and installer smoke passes. Interactive Windows Terminal behavior and deeper process/input parity still need live verification. |

Update a row only after checking the matching Herdr code and testing the
corresponding Spindle behavior. Any deliberate difference needs a reason and a
usable alternative, not just a missing implementation.
